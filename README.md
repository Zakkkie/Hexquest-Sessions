
# HexQuest Economy

## Обзор
HexQuest Economy — это стратегическая игра на шестиугольной сетке, ориентированная на баланс между экспансией, управлением ресурсами (монеты/ходы) и оптимизацией путей. Игра построена на процедурной генерации мира и соревновании с искусственным интеллектом.

## Последние обновления (v2.0)
*   **Мульти-бот режим**: Теперь можно выбрать от **1 до 3** противников (Sentinel AI) перед началом матча.
*   **Умные противники**: Боты имеют уникальные цвета (Красный, Оранжевый, Фиолетовый, Розовый) и учитывают позиции друг друга, чтобы избегать столкновений.
*   **Улучшенный UI**: 
    *   Адаптивный интерфейс для мобильных устройств.
    *   Кнопка **UPGRADE** теперь подсвечивается желтым цветом (как монеты), когда доступно повышение максимального уровня сектора.
    *   Показатели прогресса и таймеры встроены в кнопку действия.

## Основные механики

### 1. Перемещение (Movement)
Игрок перемещается по сетке шестиугольников (гексов).
*   **Стоимость хода**:
    *   Если уровень гекса 0 или 1: **1 ход**.
    *   Если уровень гекса ≥ 2: **Стоимость равна Уровню гекса**.
    *   *Пример: Переход на гекс 5-го уровня стоит 5 ходов.*
*   **Конвертация валюты**: Если у игрока недостаточно ходов, используются Монеты (Credits) по курсу `2 Монеты за 1 Ход`.
*   **Ограничение Ранга**: Игрок не может войти в гекс, максимальный уровень которого превышает текущий глобальный ранг игрока (Player Level).

### 2. Рост и Уровни (Growth & Levels)
Гексы можно "выращивать" (Growth), чтобы повышать их уровень.
*   **Время роста**: `Целевой Уровень * 5 секунд`.
*   **Условия для роста**:
    1.  **Глобальный Ранг**: Чтобы вырастить гекс до уровня `N`, ваш ранг должен быть не меньше `N-1`.
    2.  **Цикл (Cycle Lock)**: Для предотвращения "фарма" одной клетки, существует механика очереди. Вы должны улучшить/захватить 3 **разных** сектора, прежде чем сможете снова улучшать первый.

### 3. Экономика и Награды
Монеты (Credits) — основная валюта для покупки дополнительных ходов.
*   **Захват нового уровня (New Record)**: Если вы поднимаете уровень гекса до значения, которого он еще не достигал:
    *   Награда: `Уровень^2` монет.
*   **Повторное улучшение (Re-leveling)**: Если вы восстанавливаете уровень гекса (после сброса при перемещении):
    *   Награда: `Уровень` монет.

### 4. Победа
Условия победы выбираются перед началом сессии:
*   **Wealth**: Накопить заданное количество монет (например, 1000).
*   **Domination**: Достичь определенного Глобального Ранга (например, Level 10).

---

## Запуск игры

### Требования
Для работы игры **необходимо** установить **Node.js** (версия 18 или выше).
👉 [Скачать Node.js](https://nodejs.org/)

### Вариант А: Быстрый запуск (ПК)
После установки Node.js, для запуска игры:

*   **Windows**: Дважды кликните на файл `start_game.bat`.
*   **macOS / Linux**: Запустите файл `start_game.sh` через терминал. 
    *   *Примечание:* Перед первым запуском может потребоваться дать права на выполнение: `chmod +x start_game.sh`.

Эти скрипты автоматически:
1. Проверят наличие Node.js.
2. Установят необходимые библиотеки (если это первый запуск).
3. Откроют игру в вашем браузере по умолчанию.

### Вариант Б: Через терминал (для разработчиков)

```bash
# 1. Установка зависимостей
npm install

# 2. Запуск в режиме разработки
npm run dev
```

После запуска игра будет доступна по адресу: `http://localhost:5173`.
Для игры с мобильного телефона, убедитесь, что ПК и телефон находятся в одной Wi-Fi сети, и используйте IP-адрес (`Network`), который будет показан в окне терминала после запуска.
